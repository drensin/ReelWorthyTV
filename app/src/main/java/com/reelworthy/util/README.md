# Utilities (`com.reelworthy.util`)

General utility classes and extension functions.

## Components
*   **`TimeUtils`**: Helper methods for time and duration manipulation.
    *   `formatIsoDuration(String)`: Parses YouTube's ISO 8601 duration format (e.g., "PT1H10M") into a user-friendly string (e.g., "1:10").
